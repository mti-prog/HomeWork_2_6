<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/frame_def"
    android:layout_margin="16dp"
    android:elevation="2dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:id="@+id/iv_food_cate"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:layout_marginHorizontal="40dp"
        android:layout_marginVertical="16dp"
        tools:src="@drawable/ic_gray_hamburger"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <TextView
        android:id="@+id/tv_food_cate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@string/burger"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/iv_food_cate"
        android:layout_marginBottom="16dp"
        android:textColor="@color/gray"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>